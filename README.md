# 🦑 서해 워킹 쭈꾸미 원정 2026 — 회원 전용 계획 페이지

2026년 10월 서해 워킹 쭈꾸미 + 차박 원정을 위한 회원 전용 페이지입니다.
날짜·장소·숙박·회비를 **투표**로 정하고, 결과가 모든 회원에게 실시간 공유됩니다.

👉 https://shake777.github.io/tsuri-trip-2026/

## 사용법

1. 위 링크 접속 → 본인 이름 클릭 → 비밀번호 6자리 입력
2. **🗳️ 투표** 탭에서 항목 클릭 (다시 누르면 취소)
3. 끝. 저장 버튼 누를 필요 없이 자동으로 클라우드에 저장됩니다

## 탭 구성

| 탭 | 내용 |
|---|---|
| 🏠 홈 | D-DAY 카운트다운 · 회원별 진행 현황 · 단톡방 논의 요약 |
| 📍 후보지 | 장소 후보 7곳 장단점 비교 · 서해 어종 시즌 캘린더 |
| 📅 일정 | 2박 3일 표준 타임라인 · 물때/안전 체크 |
| 🗳️ 투표 | 참가 · 날짜 · 장소 · 숙박 · 낚시방식 · 회비 · 먹거리 (7종) |
| ✅ 확정 | 투표 집계 1위 · 회원별 선택 현황 |
| 💬 토론 | 스레드 + 댓글 게시판 |
| 🎒 준비물 | 5개 분야 체크리스트 (회원별 개별 체크) |
| 👥 회원 | 장비·차량 등록 (카풀 조 편성 / 스피닝 릴 대여용) |

## 투표 규칙

- **참가 여부만 단일 선택**, 나머지는 전부 **복수 선택**
- 가능한 날짜를 여러 개 고르면 겹치는 날로 자동 집계
- **불참(❌)** 으로 표시한 회원의 표는 집계에서 제외
- 동률이면 "재투표 권장" 안내 표시

## 데이터 저장 방식

**Firebase Realtime Database** + 각 회원 브라우저의 `localStorage`.

- 투표·댓글·체크·장비 변경 시 **바뀐 항목만** 즉시 기록 (예: `trip2026/votes/dates/홍길동`)
  → 여러 명이 동시에 눌러도 서로 덮어쓰지 않습니다
- 다른 회원 화면은 **15초마다 자동 반영** (탭을 다시 열면 즉시 갱신)
- Firebase가 비어 있으면 이 저장소의 `data.json`을 최초 1회 시드로 읽어옵니다
- 연결 실패 시 화면에 경고가 뜨고 `localStorage`에만 저장됩니다 (조용히 유실되지 않음)

> 이전에 쓰던 GitHub 토큰 방식은 GitHub이 공개 저장소의 토큰을 자동 폐기하기 때문에
> 주기적으로 저장이 끊겼습니다. Firebase는 URL이 공개되는 것을 전제로 설계된 서비스라
> 같은 문제가 생기지 않습니다.

## ⚙️ Firebase 연결 — **설정 완료** ✅

| 항목 | 값 |
|---|---|
| 프로젝트 | `tsuri-trip-2026` (Spark 무료 요금제, 결제수단 미등록) |
| DB 위치 | 싱가포르 (asia-southeast1) |
| DB 주소 | `https://tsuri-trip-2026-default-rtdb.asia-southeast1.firebasedatabase.app` |
| 데이터 경로 | `trip2026` |
| 규칙 | `trip2026`만 읽기/쓰기 허용, 나머지 전부 차단 |

Google 애널리틱스 · Gemini · 개발자 프로그램은 모두 **꺼둔** 상태입니다.

<details>
<summary>처음부터 다시 설정하는 방법 (펼치기)</summary>


1. [console.firebase.google.com](https://console.firebase.google.com/) → **프로젝트 만들기**
   (이름 예: `tsuri-trip-2026`, Google 애널리틱스는 사용 안 함)
2. 왼쪽 메뉴 **빌드 → Realtime Database → 데이터베이스 만들기**
   - 위치: **asia-southeast1 (싱가포르)**
   - 보안 규칙: **잠금 모드로 시작** (테스트 모드는 30일 뒤 만료되므로 피할 것)
3. 생성 후 표시되는 **주소 복사**
   `https://○○○-default-rtdb.asia-southeast1.firebasedatabase.app`
4. **규칙** 탭 → 아래로 교체 후 **게시**

   ```json
   {
     "rules": {
       ".read": false,
       ".write": false,
       "trip2026": { ".read": true, ".write": true }
     }
   }
   ```

5. `index.html` 상단 한 줄에 주소를 붙여넣고 커밋

   ```js
   const FIREBASE_DB_URL = 'https://○○○-default-rtdb.asia-southeast1.firebasedatabase.app';
   ```

</details>

설정이 풀리면 사이트에 "⚙️ 실시간 공유가 아직 켜지지 않았습니다" 경고가 뜨고,
각자 브라우저에만 저장됩니다. 같은 안내는 사이트 **[👥 회원] 탭 → 실시간 동기화 설정**에도 있습니다.

### 보안 참고

- 위 규칙은 **주소를 아는 사람은 누구나 읽고 쓸 수 있는** 설정입니다.
  이 사이트는 `robots.txt` + `noindex`로 검색에 안 잡히고 주소도 단톡방에만 공유하므로
  친목 모임용으로는 충분하지만, 더 잠그려면 Firebase 익명 인증을 추가하면 됩니다.
- Firebase URL은 비밀이 아닙니다. 페이지에 그대로 적어도 되는 값입니다.

## 후보지 요약

| # | 장소 | 서울 기준 | 비용 | 비고 |
|---|---|---|---|---|
| 1 | 안면도 무지개좌대 (고남 구매항) | 약 3시간 | 야간 5만원/인 | 방갈로 무료 · 예약 필수 |
| 2 | 안면도 백사장항·영목항 워킹 | 약 2시간 40분 | 무료 | 차박 베이스 양호 |
| 3 | 태안 신진도·안흥항 권역 | 약 2시간 30분 | 무료 | 방파제 넓음 · 선상 전환 쉬움 |
| 4 | 보령 무창포 | 약 3시간 | 무료 | 거리 이슈 의견 있음 |
| 5 | 강화 석모도 | 약 1시간 30분 | 무료 | 최근 조황 확인 필요 |
| 6 | 화성 궁평항·전곡항 | 약 1시간 20분 | 무료 | 수도권 최근접 |
| 7 | 인천 영흥도 | 약 1시간 30분 | 무료 | 캠핑장 대안 풍부 |

거리·소요시간은 대략치이며, 주차·차박 가능 여부는 출발 전 현장 확인이 필요합니다.

## 참고

- 좌대 정보 출처: [무지개좌대 (gomugomu.pro)](https://gomugomu.pro/)
- 본 페이지는 회원 간 계획 공유 목적의 비공식 자료이며, `robots.txt` + `noindex`로 검색엔진에 노출되지 않습니다.
